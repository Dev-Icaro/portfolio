/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    dangerouslyAllowSVG: true,
    domains: ['img.shields.io', 'purecatamphetamine.github.io', 'skillicons.dev'],
  },
};

export default nextConfig;
